function city_and_state(city, state) {
    //define function, set parameters
    //let address = city + ', ' +  state.toUpperCase()
    //create variable, and set state to uppercase and put the comma and space
    let address = `${city},  ${state.toUpperCase()}`
    //can also use a template string^ using backticks,
    // and putting the dollar signs and curly braces

    {
        return address
    }
}

console.log(city_and_state('Minneapolis', 'mn'))
//make sure to test code